## Known Workloads

Populated from `sudo juso-workload-list` on first
successful audit. Update when workloads are added or removed.

| Workload | User | Port | Last Audited | Last Result |
|----------|------|------|--------------|-------------|
| (empty — populate on first certified run) | | | | |

## Audit History

| Date | Verdict | FAILs |
|------|---------|-------|
| (empty) | | |

## Environment Baselines

Recorded after first CERTIFIED audit. Used to detect drift on
subsequent runs.

Ollama version: [record on first pass]
OpenClaw version: [record on first pass]
UFW rule count: [record on first pass]
Registered workloads: [record on first pass]

## Known Issues / False Positives

[Check name]: [Condition that triggered it] — [Why it is a false
positive in this environment. Date noted.]
